#!/bin/sh
set -e

case "$1" in
    purge)
        # Remove greeter cache directory (holds the extracted embedded UI) on purge
        rm -rf /var/cache/dms-greeter 2>/dev/null || true

        ;;
esac

#DEBHELPER#

exit 0
